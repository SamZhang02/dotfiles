#!/bin/bash

git config --global user.name "SamZhang02"
git config --global user.email "sam.zijing.zhang@gmail.com"
git config --global core.editor "nvim"

git config --global alias.log "log --oneline --decorate --graph"
